pip install load_dotenv
pip install pandas
pip install lightgbm
pip install scikit-learn
pip install openai
pip install dotenv
pip install langchain chromadb tiktoken pypdf
pip install -U langchain-openai langchain-chroma langchain_community
pip install tqdm
pip install langgraph

pip install "langchain_community[playwright]"
pip install playwright
playwright install chromium
pip install -U langchain_openai
playwright install
pip install beautifulsoup4
pip install ddgs
pip install -U "langchain-core>=0.3" "langchain-community>=0.3"
pip install --upgrade langchain-community google-search-results
pip install jinja2 playwright
python -m playwright install